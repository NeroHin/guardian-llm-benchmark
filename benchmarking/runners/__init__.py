"""Runner integration layer.

V1 keeps the provider implementations in the legacy execution module to avoid
breaking existing runtime behavior while the rest of the benchmark stack moves
to configuration-driven modules.
"""
